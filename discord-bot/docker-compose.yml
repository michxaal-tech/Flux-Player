services:
  claude-discord-bot:
    build: .
    env_file: .env
    environment:
      # Inside the container the workspace always lives here; map your real
      # checkout to it below. Keep this in sync with WORKSPACE_DIR in .env.
      WORKSPACE_DIR: /workspace
    volumes:
      - ./workspace:/workspace
    restart: unless-stopped
